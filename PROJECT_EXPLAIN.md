# Django-Multi-Tenantプロジェクト

本ドキュメントは、プロジェクトの 仕様書（要件定義）＋設計書＋開発工程まとめ を統合した、全体像を示す技術文書である。SaaS 型マルチテナント環境を Django で構築する際の要件、設計、実装、テスト、運用までの流れを体系的に整理する。

## 1. 仕様書（要件定義）

### 1.1 プロジェクト概要

本プロジェクトは、サブドメインごとにテナントを分離する SaaS マルチテナント Web アプリケーション基盤を Django で構築することを目的とする。テナントごとに独立したデータベーススキーマを持ち、`public` schema と `tenant` schema を分離することで、データの独立性とセキュリティを確保する。

### 1.2 機能要件

- サブドメイン（例：`demo1.localhost`、`demo2.localhost`）ごとにテナントを識別する
- `public` schema と `tenant` schema を分離する
- テナントごとに Blog / Tasks などのアプリケーションを提供する
- テナント情報を JSON（`tenants.json`）から自動生成できる
- PostgreSQL を使用し、Docker で環境構築できる
- テナントごとに API を提供できる構造を持つ

### 1.3 非機能要件

- **データ独立性**：テナント間でデータが混在しないこと
- **拡張性**：テナント追加が容易であること
- **運用性**：Docker により環境構築が容易であること
- **保守性**：アプリケーション構造が明確であること

### 1.4 想定ユーザー

- SaaS プロダクト開発者
- マルチテナント環境を必要とする企業
- テナントごとに独立したデータ管理を行いたい組織

## 2. 設計書

### 2.1 アーキテクチャ設計

本プロジェクトは、以下の構造で設計される：

- **public schema**：テナント情報（`Tenant` / `Domain`）を管理
- **tenant schema**：各テナント固有のデータを保持
- **サブドメインルーティング**：アクセスされたドメインに応じて schema を切り替える

### 2.2 ディレクトリ構造

```text
django-multi-tenant/
├── tenants/        # テナント管理アプリ
├── blog/           # テナントごとの Blog アプリ
├── tasks/          # テナントごとの Tasks アプリ
├── core/           # 共通ロジック
├── tenants.json    # 初期テナントデータ
├── docker-compose.yml
└── manage.py
```

### 2.3 モデル設計

- **Tenant モデル**
  - `schema_name`：テナントのスキーマ名
  - `name`：テナント名
- **Domain モデル**
  - `domain`：サブドメイン
  - `tenant`：`Tenant` への外部キー

### 2.4 初期データ設計

`tenants.json` により、以下を自動生成する：

- `public` テナント
- `demo1` テナント
- `demo2` テナント

### 2.5 API / アプリ設計

- **Blog アプリ**：記事投稿・閲覧
- **Tasks アプリ**：タスク管理
- テナントごとに独立したデータを保持

## 3. 開発工程まとめ（要件定義 → 設計 → 実装 → テスト → 運用）

### 3.1 要件定義工程

- マルチテナント方式の決定（schema 分離方式）
- サブドメインルーティングの要件整理
- テナントごとのアプリ提供要件の定義

### 3.2 設計工程

- `public` / `tenant` schema の役割設計
- モデル設計（`Tenant` / `Domain`）
- ディレクトリ構造の設計
- Docker による環境設計
- `tenants.json` による初期データ設計

### 3.3 実装工程

- `tenants` アプリの実装
- `Blog` / `Tasks` アプリの実装
- サブドメインルーティングの実装
- `populate_db` コマンドの実装
- Docker + PostgreSQL の構築

### 3.4 テスト工程

- `tenants/tests.py` によるテナント作成テスト
- `blog/tests.py` による Blog アプリの動作テスト
- schema 切り替えテスト
- データ分離テスト

### 3.5 運用工程

- Docker による DB 運用
- `public` / `demo1` / `demo2` のテナント運用
- `tenants.json` によるテナント追加
- Django 管理画面での運用管理
